(()=>{
    let isEnable = true;
    // isEnable = "";
    // isEnable = 123;
    let isNew:boolean = false;
    console.log(isEnable);
    isNew = true;
    console.log(isNew);

    const random = Math.random();
    console.log(random);
    isNew = random > 0.5;
    console.log(isNew);

    const myBoolean: boolean = true;
})();